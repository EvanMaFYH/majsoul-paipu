{
  "name": "majsoul-paipu",
  "description": "majsoul-paipu",
  "version": "1.0.0",
  "author": "mayf",
  "private": true,
  "scripts": {
    "start:api": "lerna run start --stream --scope=majsoul-api",
    "start:web": "lerna run start --stream --scope=majsoul-web",
    "build:api": "lerna run compile --stream --scope=majsoul-api",
    "build:web:dev": "lerna run build:dev --stream --scope=majsoul-web",
    "build:web:prod": "lerna run build:prod --stream --scope=majsoul-web",
    "launch:api": "lerna run launch --stream --scope=majsoul-api",
    "launch:web:dev": "lerna run launch:dev --stream --scope=majsoul-web",
    "launch:web:prod": "lerna run launch:prod --stream --scope=majsoul-web"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
